[package]
name = "motion_macro_ui_fail_timeline_missing_steps"
version = "0.0.0"
edition = "2024"

[workspace]

[dependencies]
leptos = { version = "0.8.19", default-features = false, features = ["csr"] }
leptos_fluid_motion = { path = "../../../../", default-features = false, features = ["macros", "timeline"] }
