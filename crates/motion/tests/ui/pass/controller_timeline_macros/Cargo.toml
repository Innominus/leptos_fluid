[package]
name = "motion_macro_ui_pass"
version = "0.0.0"
edition = "2024"

[workspace]

[dependencies]
leptos = { version = "0.8.15", default-features = false, features = ["csr"] }
leptos_fluid_motion = { path = "../../../../", default-features = false, features = ["macros", "timeline"] }
